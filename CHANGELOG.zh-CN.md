# 更新日志

[English](CHANGELOG.md) · [简体中文](CHANGELOG.zh-CN.md)

从 1.4.1 版本开始，在此记录面向用户的改动。
更早版本请查看 [GitHub Releases](https://github.com/adoin/git-Agent/releases)。

## 1.4.5 — 2026-09-07

### 改进

- Git Agent Diff 改用与合并工具一致的无边框外壳，包括圆角阴影、自绘窗口按钮、无分割线区域，以及窗口内的中英文和深色 / 亮色切换。

### 修复

- 即使仓库启用了 `core.quotepath=true`，工作区状态、暂存与丢弃、提交详情与文件搜索、补丁创建、撤销状态检查、冲突处理和 AI 合并上下文也会始终保留真实可操作的中文及其他非 ASCII 文件路径。
- 机器可读的 Git 路径输出统一改为 NUL 分隔解析，并正确处理重命名与复制记录，不再把 Git 用于展示的转义文本当作真实路径。无法使用 NUL 分隔的补丁头则通过专用的 Git C 风格路径反转义器解析。
- Git Agent Diff 中新增、删除、重命名和冲突文件的中文及其他非 ASCII 路径现在可以正常显示并匹配语法高亮。

## 1.4.4 — 2026-09-04

### 新增

- 启动时自动检查更新，默认开启，可在常规设置中关闭。后台静默检测，失败不打扰操作，不自动下载或安装。
- 仅发现新版本时，在顶部显示醒目的下载箭头更新按钮。点击查看更新详情，不主动弹窗，也不显示悬停提示。

### 改进

- 界面语言选择移到常规设置的第一行。
- 更新检测请求最多等待 20 秒，帮助菜单仍保留手动检查更新入口。

## 1.4.3 — 2026-09-04

### 修复

- “取消搜索”恢复使用统一按钮样式，保留取消和禁用状态行为，修复导致 1.4.2 安装包构建失败的回归问题。本版包含下方的搜索增强与布局优化。

## 1.4.2 — 2026-09-04

此版本安装包构建失败，请使用 1.4.3 或更高版本获取以下改进。

### 改进

- 增强提交搜索能力：文件变化搜索先显示文件名匹配，再补齐内容匹配，支持真正取消并清理 Git 进程。进度和错误提示跟随界面语言，慢搜索上限延长至 120 秒，未完成时明确标记部分结果。
- 优化提交详情卡片：历史和搜索中的提交标题、分支列表最多显示两行，悬停以单个可滚动提示查看完整内容，避免挤占文件列表空间。
- 优化部分布局展示：加高提交搜索输入框，统一提交框中 AI 生成、历史和提交选项的垂直对齐，并缩小 AI 按钮。

### 修复

- 修正 logo 分支与节点的连接处，恢复完整的空心圆环；窗口、安装包和官网图标资源统一使用同一份 SVG 源图。

## 1.4.1 — 2026-09-04

### 新增

- 根据暂存改动生成 AI 提交信息，包含规范的提交类型前缀和面向业务语义的编号改动说明。
- AI 可按需读取相关已跟踪文件、搜索符号引用，获取暂存差异之外的上下文。分析基于固定的暂存区树快照，不混入未暂存修改或未跟踪文件。
- AI 提交信息支持独立的英文 / 中文设置，不受应用界面语言影响。
- 简体中文 README 和语言切换入口，以及可从 README 和官网访问的中英文更新日志。

### 改进

- AI 生成改为点击即开始，悬停显示当前模型及数据发送提示；生成完成后直接写入提交框，不再经过二次确认或采用建议的步骤。
- 保留生成期间手动编辑的草稿，支持取消生成；暂存区或 HEAD 变化时结果失效。不会自动提交或推送。
- AI 生成入口采用更醒目的图标按钮；主按钮的禁用样式与提交按钮统一，包括图标和文字颜色。

### 许可证

- 补充 Apache License 2.0 与 Commons Clause License Condition v1.0 的组合条款、署名声明及发布包内的许可证文件。两者共同适用；项目为源码可见软件，并非未经修改的 Apache 2.0 授权。详见 [LICENSE](LICENSE) 和 [NOTICE](NOTICE)。
